[project]
name = "trading-goat"
version = "2.0.0"
description = "Real-time AI-assisted paper-trading platform (paper only)"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 110

[tool.pytest.ini_options]
testpaths = ["tests"]
